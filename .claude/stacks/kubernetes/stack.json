{
  "name": "kubernetes",
  "version": "1",
  "detect": [
    { "file": "k8s", "type": "dir-marker" },
    { "file": "manifests", "type": "dir-marker" },
    { "file": ".kube", "type": "dir-marker" }
  ],
  "path_hints": ["/k8s/", "/manifests/", "/.kube/"],
  "gates": {
    "kubeconform": {
      "enabled": false,
      "severity": "warn",
      "trigger": ["verify", "commit"],
      "command": "kubeconform -summary -ignore-missing-schemas {files} 2>&1 | head -30",
      "description": "K8s manifest schema validation (kubeconform — the maintained successor to the archived kubeval). Schema only, NOT policy-as-code (kube-score / OPA Gatekeeper / Kyverno NOT included). -ignore-missing-schemas lets CRDs without a published schema pass the lint instead of failing it.",
      "stacks": ["kubernetes"],
      "timeout": 60
    }
  }
}
